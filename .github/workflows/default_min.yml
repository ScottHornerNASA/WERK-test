# This is a basic workflow to help you get started with Actions

name: test1

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    env:
      SMTP_SERVER: mail.sonic.net
      SMTP_PORT: 587
      MAIL_SUBJECT: "GitHub mail"
      MAIL_BODY: "Sent and email"
      FROME: "shorner@sonic.net"
      TO: "zephod4321@gmail.com"
  
    steps:
      # - name: Checkout (optional if you don’t need repo files)
      #  uses: actions/checkout@v4

      - name: Send email via SMTP
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ env.SMTP_SERVER }}
          server_port: ${{ env.SMTP_PORT }}
          secure: false                  # true if your server requires TLS on connect; with STARTTLS use false
          username: shorner
          password: "9iuy%^TTGG"
          subject: ${{ env.MAIL_SUBJECT }}
          to: ${{ env.TO }}
          from: ${{ env.FROM }}
          body: ${{ env.MAIL_BODY }}

